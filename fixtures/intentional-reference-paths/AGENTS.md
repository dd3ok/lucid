# AGENTS.md

Read `memory/YYYY-MM-DD.md` for today's daily note.

Use `$HOME/.watchlist/WATCHLIST.md` for repo-independent personal follow-ups.

Use `${HOME}/.config/lucid/notes.md` for home-scoped notes in examples.

Optional examples can use `docs/<project>/README.md` as a placeholder path.
