interface:
  display_name: "Lucid"
  short_description: "Audit agent context and cleanup prompt debt."
  default_prompt: "Use $lucid to audit this repository for stale, contradictory, unsafe, or obsolete agent-facing context."
